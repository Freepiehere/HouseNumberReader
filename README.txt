 * Introduction:
  project in computer vision. Segmented pictures of house numbers are fed into a convolutional neural network (CNN) which returns the numbers
  captured within the picture.

* Multisegment:
  A dataset handler and model trainer which uses multithreading in order to prepare training samples while training takes place.

  ###################
  More Details to come